# SVGER v4.1 Compatibility Ledger

Status: **Pending maintainer approval**

Baseline: `baselines/v4.0.x/` captured from v4.0.8 (`0d48aff`)

Machine-readable differential report: `compatibility/dual-path-diff-v4.0.8.json`

This ledger classifies the divergences and known quirks that constrain the Phase 1 unification.
Phase 0 security containment takes precedence where the old behavior was unsafe.

| ID       | Observed divergence                                                                                                 | Selected canonical behavior                                                                                                                                                                          | Breaking?                                                             | Classification        | Rationale                                                                                    | User impact                                                                                               | Migration mechanism                                                                                                                                  | Deprecation timeline                                                                                     | Contract test                                                                                                                       | Approver                                               |
| -------- | ------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| COMP-001 | Legacy `buildAll` writes components only; `SVGService.buildAll` also creates an index for supported frameworks.     | The unified application service creates a planned, owned index only for adapters that support barrel exports. Both CLI and facade use that plan.                                                     | Yes, for legacy callers that did not receive an index.                | `auto-migrate`        | The service behavior is already the CLI behavior and is required for a single build plan.    | `SVGER.build()` may add an owned `index.ts`/`index.js`.                                                   | v4.1 migration report previews the added artifact; ownership metadata prevents unrelated overwrite.                                                  | Facade warns on first use in v4.1; facade removed in v5.0.                                               | Planned: `tests/phase1/builder-facade.contract.test.ts`; existing evidence: `tests/phase0/dual-path-characterization.mjs`.          | Pending: compatibility owner and release owner.        |
| COMP-002 | Shared component names and bytes match for the canonical fixtures and configuration.                                | Preserve byte content and component naming when no separately approved change applies.                                                                                                               | No.                                                                   | `preserve`            | This is the stable common behavior of both paths.                                            | Safe v4 sources keep the same generated component.                                                        | No action; differential fixtures remain a release contract.                                                                                          | Permanent v4.1 compatibility contract.                                                                   | `tests/phase0/dual-path-characterization.mjs`.                                                                                      | Pending: compatibility owner.                          |
| COMP-003 | Legacy diagnostics are direct emoji console messages; service diagnostics are timestamped logger messages.          | Versioned `BuildReport` diagnostics are canonical; pretty output is a renderer over the same diagnostic records.                                                                                     | Yes, for consumers parsing console text.                              | `deprecate`           | Stable machine-readable diagnostics cannot depend on presentation text.                      | Exact legacy log strings and timestamps are not guaranteed.                                               | Use `--format json` or `--format ndjson`; migration notes map legacy messages to diagnostic codes.                                                   | Legacy presentation deprecated in v4.1 and removed with the facade in v5.0; first facade use warns once. | Planned: `tests/phase1/report-formats.contract.test.ts`.                                                                            | Pending: CLI/API owner.                                |
| COMP-004 | Successful service jobs retain a timer and can keep a programmatic process alive for about 30 seconds.              | Completed jobs release all lifecycle resources immediately; report metrics are retained in the returned report, not a timer-backed global queue.                                                     | Yes, but only for callers relying on accidental event-loop retention. | `intentionally-break` | Event-loop retention is a correctness defect and conflicts with scoped contexts.             | Programs terminate promptly after work completes.                                                         | None required; callers needing persistence must explicitly keep their process alive.                                                                 | Removed in v4.1 with no shim.                                                                            | Planned: `tests/phase1/lifecycle.contract.test.ts`.                                                                                 | Pending: application-service owner.                    |
| COMP-005 | Legacy builder aborts at the first processing error; the service gathers failures and normally resolves.            | Discovery and planning failures reject before mutation; per-file execution completes according to failure policy and returns one `BuildReport`; CLI exits nonzero when the report contains failures. | Yes.                                                                  | `auto-migrate`        | A report-based contract preserves all failures while retaining deterministic mutation rules. | Programmatic callers receive a report instead of relying on first-error throws.                           | Facade delegates to the service, returns the report, and documents exception boundaries; ignored return values remain source-compatible.             | `Promise<void>` behavior deprecated in v4.1; facade removed in v5.0.                                     | Planned: `tests/phase1/build-failure-report.contract.test.ts`.                                                                      | Pending: API owner.                                    |
| COMP-006 | Shallow runtime/file configuration merges can discard nested sibling values.                                        | Schema-aware deep merge with value-origin tracking is canonical.                                                                                                                                     | Yes, where callers intentionally depended on sibling loss.            | `deprecate`           | Sibling loss is unsafe and violates Phase 1 configuration requirements.                      | Nested overrides retain unspecified siblings.                                                             | `svger config explain` shows origins; `svger migrate config --dry-run` previews changed values.                                                      | Shallow merge documented as deprecated in v4.0.9 and removed in v4.1.                                    | Planned: `tests/phase1/config-merge.contract.test.ts`.                                                                              | Pending: configuration owner.                          |
| COMP-007 | Discovery is non-recursive, case-sensitive, filesystem-order dependent, and inconsistently applies include/exclude. | One stable, normalized discovery implementation with explicit recursion, glob, hidden-file, symlink, output-exclusion, and count policies.                                                           | Yes.                                                                  | `deprecate`           | Determinism and global planning require one ordered source set.                              | Some previously ignored uppercase or nested SVGs may be discovered when configured; order becomes stable. | Migration report lists newly included/excluded files; compatibility defaults preserve non-recursive behavior unless the user opts in where feasible. | Legacy discovery deprecated in v4.0.9 and replaced in v4.1.                                              | Planned: `tests/phase1/discovery.contract.test.ts`.                                                                                 | Pending: compiler owner.                               |
| COMP-008 | Unsafe scripts, event handlers, and JavaScript URIs were embedded in generated output.                              | Reject with `E_UNSAFE_SVG_CONTENT`; strip only by explicit option with a prominent warning.                                                                                                          | Yes, security fix.                                                    | `intentionally-break` | Unsafe acceptance cannot be preserved.                                                       | Unsafe sources stop the affected build before their artifact is written.                                  | Fix the source or explicitly use temporary strip mode; migration notice documents both.                                                              | Effective in v4.0.9; never removed or relaxed implicitly.                                                | `tests/phase0/security-containment.test.mjs`.                                                                                       | Pending: security reviewer.                            |
| COMP-009 | No raw SVG byte-size ceiling existed.                                                                               | Enforce a configurable 10 MiB default and fail with `E_SVG_INPUT_TOO_LARGE`.                                                                                                                         | Yes, security/capacity fix.                                           | `intentionally-break` | Unbounded input can exhaust memory before parsing.                                           | Oversized SVGs are rejected.                                                                              | Optimize/split the source or explicitly set a reviewed positive byte limit.                                                                          | Effective in v4.0.9; no compatibility shim.                                                              | `tests/phase0/security-containment.test.mjs`.                                                                                       | Pending: security reviewer.                            |
| COMP-010 | Artifact paths were joined without verified root containment and existing symlink destinations could be followed.   | Every artifact is resolved inside its designated output root; lexical, physical-parent, and existing-symlink escapes fail with `E_OUTPUT_PATH_ESCAPE`.                                               | Yes, security fix.                                                    | `intentionally-break` | Writes outside the declared root are never a supported compatibility requirement.            | Unsafe layouts fail without modifying the external target.                                                | Remove the symlink or choose the intended containing output root.                                                                                    | Effective in v4.0.9; no compatibility shim.                                                              | `tests/phase0/security-containment.test.mjs`.                                                                                       | Pending: security reviewer.                            |
| COMP-011 | Jest transformation errors could silently return a mock, including for unsafe input.                                | Raw input security diagnostics run before mock/fallback and always propagate.                                                                                                                        | Yes, security fix.                                                    | `intentionally-break` | A test-only fallback must not hide a security violation.                                     | Unsafe Jest fixtures fail the test transformation.                                                        | Fix the fixture or explicitly select strip policy; normal non-security mock mode remains supported.                                                  | Effective in v4.0.9; never relaxed.                                                                      | `tests/phase0/security-containment.test.mjs`.                                                                                       | Pending: Jest integration owner and security reviewer. |
| COMP-012 | The custom CLI parser exposes kebab-case keys while handlers inconsistently read camel-case multiword flags.        | Phase 1 command parsing normalizes declared options to typed names while retaining documented v4 spellings as aliases.                                                                               | Potentially, for scripts exploiting ignored/broken flags.             | `deprecate`           | Typed command validation requires one normalized representation.                             | Previously ineffective flags begin behaving as documented; unknown spellings become usage errors.         | `svger migrate` reports obsolete spellings; supported aliases warn once and identify their replacement.                                              | Aliases warn in v4.1 and are removed in v5.0.                                                            | Planned: `tests/phase1/cli-options.contract.test.ts`; Phase 0 safety flags covered by `tests/phase0/security-containment.test.mjs`. | Pending: CLI owner.                                    |

## Approval gate

Phase 1 work must not merge until maintainers approve this ledger after reviewing the baseline and
differential report. Approval should record reviewer, date, and the exact baseline lock hash in the
release record.

Baseline lock SHA-256: `4671b05099779bbafb42b9b91d1ea6332f4033d89d28744960e62a37a9252cc4`

| Review                 | Reviewer | Date | Decision | Evidence / notes                                                |
| ---------------------- | -------- | ---- | -------- | --------------------------------------------------------------- |
| Baseline approval      | Pending  | —    | Pending  | 14/14 locked artifacts verified.                                |
| Security containment   | Pending  | —    | Pending  | Core assertions and entry-point matrix pass locally.            |
| Compatibility approval | Pending  | —    | Pending  | Each divergence requires row-level acceptance.                  |
| Release gate sign-off  | Pending  | —    | Pending  | Requires clean checkout and independently verified publication. |
